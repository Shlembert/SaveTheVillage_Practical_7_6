public enum TypeUnit
{
    Farmer,
    Warrior,
    Enemy,
    Food
}

public enum StatusWarrior
{
    Patrol,
    Follow,
    Battle,
    Escape
}

public enum StatusFarmer
{
    GoGarden,
    Work,
    GoToStorage,
    Panic
}

public enum StatusEnemy
{
    Follow,
    EquipFarmer,
    EquipGrain,
    GoToStorage,
    Escape
}